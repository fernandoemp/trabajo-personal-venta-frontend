import { FilterDniPipe } from './filter-dni.pipe';

describe('FilterDniPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDniPipe();
    expect(pipe).toBeTruthy();
  });
});
